// ディープリサーチ用のプロンプト構築（ai-incho から移植）
// ※ STEP 1 ではリサーチ実行用 buildResearchPrompt のみ。学習資料生成プロンプトは STEP 2 で追加する。

import type { ResearchMode, ResearchPerspective } from "./types";

/** 視点別の追加指示（医療・研修現場で即使える調整） */
const PERSPECTIVE_INSTRUCTIONS: Record<ResearchPerspective, string> = {
  general: "",
  medical:
    "PMDA添付文書・日本皮膚科学会ガイドライン・査読論文を最優先の根拠とすること。エビデンスレベルを明記。憶測や不確実な情報は避け、根拠が不明な点は『要確認』と明示すること。2024〜2026年の最新情報を優先。",
  training:
    "南草津皮フ科のスタッフ（マルチタスク医療事務・看護師）が日々の業務で使える実践的な形にまとめること。具体的な手順・確認ポイント・注意事項を含める。専門用語には簡単な補足を付ける。",
  patient:
    "患者さんにそのまま説明できる平易な言葉で。専門用語は避けるか言い換える。不安を和らげる配慮ある表現を使う。",
};

/** 全視点共通でリサーチ末尾に添えるクリニック理念の視点（簡潔に） */
const PHILOSOPHY_NOTE =
  "※南草津皮フ科は『患者様の人生好転・四方よし』を理念とする保険×美容のハイブリッドクリニックです。この視点も踏まえてまとめてください。";

export function buildResearchPrompt(params: {
  topic: string;
  mode: ResearchMode;
  perspective?: ResearchPerspective;
  additionalContext?: string;
}): string {
  const { topic, mode, perspective = "general", additionalContext } = params;

  const modeInstructions: Record<ResearchMode, { length: string; detail: string }> = {
    quick: {
      length: "1500 字程度",
      detail: "要点を簡潔に",
    },
    standard: {
      length: "3000 字程度",
      detail: "主要な内容を詳しく",
    },
    deep: {
      length: "5000 字以上",
      detail: "網羅的かつ詳細に、各セクションを深く掘り下げる",
    },
  };

  const config = modeInstructions[mode] ?? modeInstructions.standard;
  const perspectiveNote = PERSPECTIVE_INSTRUCTIONS[perspective] || "";

  return `# ディープリサーチ依頼

## トピック
${topic}

## 求める分量
${config.length}

## 詳細度
${config.detail}

## 必須要件(極めて重要)
- **必ず最後まで完結させる**こと。途中で終わるのは絶対に避ける
- 文字数より**完結性を最優先**する
- 必ず以下の構成で書く:
  1. はじめに(トピックの定義と重要性)
  2. 背景と概要
  3. 詳細解説(複数セクション)
  4. 実践・活用のポイント
  5. **まとめ・結論(必ず書く)**
  6. 参考資料・情報源

## 情報の質
- 最新情報を含めてください
- 情報源を明示してください(学会名、論文名、公的機関名など)
- エビデンスに基づいた記述を心がけてください
- 推測や個人的見解は明示してください

${perspectiveNote ? `## リサーチの視点（最優先で反映）\n${perspectiveNote}` : ""}

${additionalContext ? `## 追加要件\n${additionalContext}` : ""}

## クリニックの視点
${PHILOSOPHY_NOTE}

## 出力形式
Markdown 形式で出力してください。見出し(##, ###)、箇条書き、強調(太字)を適切に使ってください。`;
}

// ─────────────────────────────────────────────────────────────
// STEP 2: 学習資料の生成プロンプト（ai-incho から移植・Markdown出力）
//   共通の厳守事項:
//   - 読者は医療スタッフ（看護師・医療事務・エステティシャン等）
//   - 略語は初出時にフル展開（例: TCS = Topical Corticosteroid）
//   - 「絶対」「100%」「日本一」「最高」等の断定/最大級表現は使わない
//   - 出力は本文のみ。前置きやコードフェンス（```md 等）は付けない
// ─────────────────────────────────────────────────────────────

/** 学習資料の種類 */
export type LearningDocType = "training" | "knowledge" | "quiz" | "summary";

/** 知識シートのレベル */
export type KnowledgeLevel = "basic" | "expert";

/** 研修資料（スタッフ向け要点整理・Markdown） */
export function getTrainingDocPrompt(topic: string, researchText: string): string {
  return `あなたはクリニックの医療スタッフ向け教育資料を作成する専門家です。
以下のリサーチ結果から、スタッフ向けの「要点整理 研修資料」を **Markdown** で作成してください。

【元のリサーチテーマ（参考）】
${topic}

【リサーチ結果】
${researchText}

【作成方針】
- 読者は医療スタッフ（看護師・医療事務・エステティシャン等）。専門用語OK（略語は初出時にフル展開）
- 「量より要点」「視覚的にスキャンしやすい」を最優先
- 暗記すべき数字・固有名詞は **太字** でマーキング
- 全体で 800〜1100 字に収める

【出力フォーマット — このMarkdown構成】
# （短いタイトル）— スタッフ向け要点
（タイトルは治療名/疾患名＋「— スタッフ向け要点」など全角25字以内）

## 1. 概要・ポイント
- **定義/特徴:** （1〜2行）
- **主な対象患者:** （1〜2行）
- **重要な数字:** （有病率・効果発現時期など 1〜2行）

## 2. 診療フロー/治療フロー
1. **（ステップ1）:** （簡潔に）
2. **（ステップ2）:** （簡潔に）
3. **（ステップ3）:** （簡潔に）
4. **（ステップ4）:** （簡潔に）

## 3. スタッフが押さえるべき注意点
- （注意1）
- （注意2）
- （注意3）

## 4. 患者対応のコツ
- **よくある質問への回答例:** （簡潔に）
- **不安を和らげる言葉がけ:** （簡潔に）
- **医師への引き継ぎが必要なケース:** （簡潔に）

## 5. 覚えておきたい一言メモ
（現場で役立つ知識を1〜2文・暗記推奨ポイント）

【厳守】数値・固有名詞は太字。断定/最大級表現は使わない。出力はMarkdown本文のみ（前置き・コードフェンス禁止）。`;
}

/** 知識シート（初心者版/エキスパート版・Markdown） */
export function getKnowledgeSheetPrompt(
  topic: string,
  researchText: string,
  level: KnowledgeLevel
): string {
  if (level === "expert") {
    return `あなたはクリニックの看護師・医療事務スタッフ教育の専門家です。
以下のリサーチ結果から、**ベテランスタッフ向けの「知識UPシート（エキスパート版）」** を **Markdown** で作成してください。

【テーマ】
${topic}

【リサーチ結果】
${researchText}

【対象読者】経験のある看護師・医療事務。患者説明や新人指導の立場。最新の医学情報・ガイドラインを学びたい。

【出力フォーマット — このMarkdown構成】
# 🏆 （テーマ名）— エキスパート向け 知識深堀シート

## 🎯 1. 概要・最新動向
（分野の現状と最近の動向 4〜5文）

## 🔬 2. 病態・治療メカニズムの詳細
- （ポイント1）
- （ポイント2）
- （ポイント3）

## 📚 3. 最新ガイドライン・エビデンス
- **（ガイドライン名・治療法）:** （内容・推奨度）
- **（別の最新トピック）:** （内容）

## 💊 4. 治療の選択肢と使い分け
- **（症例パターン1）:** （推奨される対応）
- **（症例パターン2）:** （推奨される対応）

## 💬 5. 患者さんへの説明スキル
**Q. （本質に迫る質問）**
👉 説明例: （専門知識を踏まえつつ分かりやすく）
💡 訴求ポイント: （伝えるべき本質）

## 🎓 6. 新人スタッフへの指導ポイント
- **（指導ポイント1）:** （なぜ重要か＋教え方のヒント）
- **（指導ポイント2）:** （説明）

## ⚠️ 7. 注意すべきポイント・落とし穴
- （注意点1）
- （注意点2）

## ✨ まとめ・次のアクション
（2〜3文のまとめ＋さらに学ぶための提案）

【厳守】断定/最大級表現や他院を貶める表現は禁止。「個人差があります」を要所で。ですます調。最新の医学情報を含める。全体3000字以内。出力はMarkdown本文のみ（前置き・コードフェンス禁止）。`;
  }

  return `あなたはクリニックの看護師・医療事務スタッフ教育の専門家です。
以下のリサーチ結果から、**新人スタッフ向けの「知識UPシート（初心者版）」** を **Markdown** で作成してください。

【テーマ】
${topic}

【リサーチ結果】
${researchText}

【対象読者】クリニックの新人看護師・新人医療事務。専門知識が浅い。患者さんから質問されたとき「どう答えればいいか」が分かるレベル。中学生でも理解できる平易な日本語。

【出力フォーマット — このMarkdown構成】
# 🌱 （テーマ名）を学ぼう — 新人スタッフ向け

## 📖 1. これってどんな病気/治療？
（やさしい言葉で3〜4文）

## 🔑 2. ここだけ押さえよう（超基本3つ）
- **（キーポイント1）:** （説明）
- **（キーポイント2）:** （説明）
- **（キーポイント3）:** （説明）

## 💡 3. よく使う言葉（かんたん辞典）
- **（専門用語1）:** （やさしい説明）
- **（専門用語2）:** （やさしい説明）

## ❓ 4. 患者さんからよく聞かれる質問
**Q. （よく聞かれる質問1）**
👉 こう答えよう: （回答例1〜2文）
💡 ポイント: （添える言葉）

## ⚠️ 5. やってはいけないこと（NG行動）
- **❌ （NG行動1）:** （なぜダメか＋代わりにどうすべきか）
- **❌ （NG行動2）:** （説明）

## ✨ まとめ
（2〜3文の励まし＋次に学ぶとよいことのヒント）

【厳守】断定/最大級表現は禁止。「個人差があります」を要所で。専門用語はやさしい言い換え併記。ですます調で温かく。全体1500字以内。出力はMarkdown本文のみ（前置き・コードフェンス禁止）。`;
}

/** クイズ（4択・解説付き・JSON出力） */
export function getQuizPrompt(
  topic: string,
  researchText: string,
  numQuestions: number = 10
): string {
  return `あなたはクリニックスタッフ向け教育テストの問題作成専門家です。
以下のリサーチ結果から、4択クイズ ${numQuestions}問を作成してください。

【元のリサーチテーマ】
${topic}

【リサーチ結果】
${researchText}

【作成方針】
- 読者は医療スタッフ
- 単純な暗記問題と状況判断問題をバランスよく混ぜる
- 各問題に必ず「解説」を付ける（1〜2行）
- 4択の中に正解は1つだけ。紛らわしいだけのトリッキー問題は避ける
- 同じ知識を繰り返す類似問題を作らない

【出力フォーマット — 必ずこのJSON形式のみ・他のテキスト一切なし】
{
  "questions": [
    {
      "q": "問題文",
      "choices": ["選択肢A", "選択肢B", "選択肢C", "選択肢D"],
      "answer_index": 0,
      "explanation": "解説1〜2行"
    }
  ]
}

合計 ${numQuestions} 問を questions 配列に入れてください。

【厳守】
- 出力は上記のJSONのみ（前置き・説明文・コードフェンス禁止）
- answer_index は 0〜3 の整数（0=A, 1=B, 2=C, 3=D）
- choices は必ず4要素
- 「絶対」「100%」「日本一」等の表現禁止`;
}

/** 要約（500〜700字・Markdown） */
export function getSummaryPrompt(topic: string, researchText: string): string {
  return `あなたは医療文書の要約専門家です。
以下のリサーチ結果を、医療従事者が短時間で要点を掴めるよう、500〜700字の日本語で **Markdown** で要約してください。

【テーマ】
${topic}

【リサーチ結果】
${researchText}

【出力フォーマット — このMarkdown構成】
## 📌 概要
（全体像を2〜3文・「である調」）

## 🔑 重要ポイント
- **（キー1）:** （簡潔な説明）
- **（キー2）:** （簡潔な説明）
- **（キー3）:** （簡潔な説明）

## 📊 数値・データ
- （発症率・効果率・推奨期間など具体的な数字）

## ⚠️ 注意点・最新動向
（2〜3文・「である調」）

【厳守】全体500〜700字。「である調」で簡潔に。数値・固有名詞は **太字**。推測や脚色を加えない。出力はMarkdown本文のみ（前置き・コードフェンス禁止）。`;
}

/**
 * 「必須のまとめ」生成プロンプト。
 * リサーチ結果から「これだけは押さえるべき必須事項」を厳選し、約1000字でまとめる。
 * ※ 生成結果は生テキスト（whitespace-pre-wrap）で表示するため、Markdown記号（#・*）は使わせず、
 *    ◆/・/【】 等で見やすく整形する。
 */
export function getEssentialsPrompt(topic: string, researchText: string): string {
  return `あなたは医療スタッフ教育の専門家です。
以下のリサーチ結果から、スタッフが「これだけは押さえるべき必須事項」を厳選し、読みやすくまとめてください。

【テーマ】
${topic}

【リサーチ結果】
${researchText}

【出力フォーマット — この体裁で（プレーンテキストとして読みやすく）】
◆ 必須のまとめ：${topic}
（このテーマの全体像と最重要点を2〜3文・「である調」）

◆ これだけは押さえる
・項目1：（要点を1〜2文で具体的に）
・項目2：（要点を1〜2文で具体的に）
・項目3：（要点を1〜2文で具体的に）
・項目4：（要点を1〜2文で具体的に）
（※ 重要なものを4〜6項目。各項目は一段詳しく説明する）

◆ 特に注意
・（現場で間違えやすい・事故につながる点を2〜3項目、それぞれ1〜2文）

【厳守】
- 全体 **約1000字（900〜1100字）**。要点は増やしてよいが冗長にしない。
- **網羅ではなく厳選**。最重要の事項に絞り、各項目の説明を一段詳しくする。
- **Markdown記号（#・##・*・**・- などの「-」箇条書き）は使わない。** 見出しは「◆」、箇条書きは「・」、必要なら【】で小見出しを表す。
- 強調したい語は「」で囲む（** は使わない）。数値・固有名詞は正確に書く。
- 推測や脚色を加えない。資料に無い固有の数値・規程は書かない。
- 出力は本文のみ（前置きの挨拶・コードフェンス禁止）。`;
}

// ─────────────────────────────────────────────────────────────
// STEP 2 拡張②③: 既存知識システムへの変換プロンプト（JSON/平易テキスト出力）
// ─────────────────────────────────────────────────────────────

/** リサーチ本文 → 組織ナレッジ（OrgKnowledge）への変換プロンプト（JSON出力） */
export function getToOrgKnowledgePrompt(topic: string, researchText: string): string {
  return `あなたはクリニックの組織ナレッジ整理の専門家です。
以下のリサーチ結果を、組織の「学び」ナレッジとして1件にまとめ、必ず指定のJSON形式のみで出力してください。

【テーマ】
${topic}

【リサーチ結果】
${researchText}

【出力JSON（このキー構成のみ・前置き/説明/コードフェンス禁止）】
{
  "title": "テーマを表す簡潔なタイトル",
  "situation": "このナレッジが役立つ背景・状況（2〜3文）",
  "content": "リサーチから抽出した要点（医療スタッフ向けに3〜5文）",
  "impact": "【患者よし】… 【スタッフよし】… 【クリニックよし】… 【社会よし】…（四方よし形式で各1文）",
  "actionItems": ["現場で実践できる具体アクション1", "アクション2", "アクション3"],
  "tags": ["キーワード1", "キーワード2", "キーワード3"]
}

【厳守】断定/最大級表現は禁止。actionItems は3件、tags は3件。出力は上記JSONのみ。`;
}

/** リサーチ本文 → 院内マニュアル（Manual）への変換プロンプト（JSON出力。knowledge-generate の manual 形式に準拠） */
export function getToManualPrompt(topic: string, researchText: string): string {
  return `あなたはクリニックの院内マニュアル作成の専門家です。
以下のリサーチ結果を、スタッフ向けの実践マニュアルに変換し、必ず指定のJSON形式のみで出力してください。

【テーマ】
${topic}

【リサーチ結果】
${researchText}

【出力JSON（このキー構成のみ・前置き/説明/コードフェンス禁止）】
{
  "title": "マニュアルのタイトル",
  "purpose": "このマニュアルの目的（1〜2文）",
  "category": "受付・会計 / 生物学的製剤 / カウンセリング / レセプト・算定 / 緊急対応 / その他 のいずれか",
  "steps": [
    { "order": 1, "title": "手順名", "description": "具体的な説明", "checkpoints": ["確認点1", "確認点2"], "tips": "コツ（任意）" }
  ],
  "todoItems": [
    { "text": "やること", "timing": "daily|weekly|monthly|asneeded|initial", "priority": "high|normal|optional" }
  ],
  "cautions": ["注意事項1", "注意事項2"],
  "faq": [ { "q": "よくある質問", "a": "回答" } ]
}

【厳守】
- steps は最大5、cautions は最大4、faq は最大3、todoItems は5〜8件。
- 断定/最大級表現は禁止。専門用語には簡単な補足を付ける。
- 出力は上記JSONのみ。`;
}

// ─────────────────────────────────────────────────────────────
// STEP 3 作業2: 疾患ディープリサーチ（検索Grounding）＋ 疾患データ更新
// ─────────────────────────────────────────────────────────────

/** 疾患リサーチに渡す既存データ（Disease の一部・任意項目） */
export type DiseaseResearchContext = {
  description?: string;
  cause?: string;
  treatment?: string;
  patientExplanation?: string;
  keyPoints?: string[];
};

/**
 * 疾患のディープリサーチ用プロンプト（検索Grounding前提）。
 * スタッフの知識・スキル向上のための研修資料を生成する。
 */
export function getDiseaseResearchPrompt(
  diseaseName: string,
  englishName: string,
  currentData: DiseaseResearchContext,
  perspective: ResearchPerspective = "training"
): string {
  const perspectiveNote = PERSPECTIVE_INSTRUCTIONS[perspective] || "";
  const known: string[] = [];
  if (currentData.description) known.push(`【概要】${currentData.description}`);
  if (currentData.cause) known.push(`【原因・誘因】${currentData.cause}`);
  if (currentData.treatment) known.push(`【現行の治療法】${currentData.treatment}`);
  if (currentData.keyPoints?.length) known.push(`【既知の重要ポイント】\n- ${currentData.keyPoints.join("\n- ")}`);
  const knownBlock = known.length ? `\n## 当院の既存情報（参考・矛盾なく拡充すること）\n${known.join("\n")}\n` : "";

  return `# 皮膚科疾患の研修リサーチ依頼

皮膚科疾患「${diseaseName}${englishName ? `（${englishName}）` : ""}」について、南草津皮フ科のスタッフ（マルチタスク医療事務・看護師・カウンセラー）の知識・スキル向上のための研修資料を **Markdown** で作成してください。
${knownBlock}
## 必ず含める内容
1. **疾患の概要・病態生理**（専門用語には補足を付け、わかりやすく）
2. **原因・誘因・リスク因子**
3. **主な治療法**（2024〜2026年の最新治療・生物学的製剤・JAK阻害薬等を含める）
4. **スタッフが患者対応で知っておくべきポイント**
5. **レセプト・保険算定で注意すべき点**（保険適用・査定リスク等）
6. **カウンセリング・患者説明のコツ**（四方よしの精神・不安を和らげる言葉がけ含む）
7. **よくある患者からの質問と回答例**（3〜5問・スタッフがそのまま使える形で）

## 情報の質（極めて重要）
- PMDA添付文書・日本皮膚科学会ガイドライン・査読論文を最優先の根拠とする
- 情報源（学会名・ガイドライン名・公的機関名）を本文中に明示する
- 根拠が不明・不確実な点は「要確認」と明示し、断定しない
- 「絶対」「100%」等の最大級表現は使わない

${perspectiveNote ? `## リサーチの視点（最優先で反映）\n${perspectiveNote}\n` : ""}
## クリニックの視点
※南草津皮フ科は『患者様の人生好転・四方よし（患者・スタッフ・クリニック・社会よし）』を理念とする保険×美容のハイブリッドクリニックです。この視点も踏まえてまとめてください。

## 出力形式
Markdown 形式（見出し ##/###・箇条書き・**強調** を適切に使用）。前置きやコードフェンスは付けず、本文のみ。`;
}

/**
 * 疾患リサーチ結果 → 疾患データ（Disease）更新案の生成プロンプト（JSON出力）。
 * description / cause / treatment / keyPoints をより充実した内容に再生成する。
 */
export function getDiseaseUpdatePrompt(
  diseaseName: string,
  currentData: DiseaseResearchContext,
  researchText: string
): string {
  return `あなたはクリニックの疾患情報データベースの編集者です。
以下のリサーチ結果をもとに、疾患「${diseaseName}」のデータ項目を、現行より充実した正確な内容に再生成し、必ず指定のJSON形式のみで出力してください。

【現行の疾患データ】
- 概要(description): ${currentData.description || "（未記入）"}
- 原因・誘因(cause): ${currentData.cause || "（未記入）"}
- 治療法(treatment): ${currentData.treatment || "（未記入）"}
- 重要ポイント(keyPoints): ${currentData.keyPoints?.length ? currentData.keyPoints.join(" / ") : "（未記入）"}

【リサーチ結果】
${researchText}

【出力JSON（このキー構成のみ・前置き/説明/コードフェンス禁止）】
{
  "description": "疾患の概要（医療スタッフ向けに2〜3文。当院のハイブリッド診療の文脈も活かす）",
  "cause": "原因・誘因・リスク因子（1〜3文）",
  "treatment": "主な治療法（保険診療＋自由診療、最新治療を含め簡潔に）",
  "keyPoints": ["スタッフが押さえるべき要点1", "要点2", "要点3", "要点4", "要点5"]
}

【厳守】
- 現行データの正しい固有名詞（薬剤名・機器名等）は引き継ぐ。リサーチで誤りが判明した場合のみ修正。
- keyPoints は4〜6件。各要点は具体的で簡潔に。
- 断定/最大級表現は禁止。根拠が不確実な内容は含めない。
- 出力は上記JSONのみ。`;
}

/** リサーチ本文 → 患者向け説明書への変換プロンプト（平易なMarkdown） */
export function getPatientSheetPrompt(topic: string, researchText: string): string {
  return `あなたは患者さんへの説明資料を作る専門家です。
以下のリサーチ内容を、患者さんにそのまま渡せる「わかりやすい説明書」に変換してください。

【テーマ】
${topic}

【リサーチ結果】
${researchText}

【作成方針】
- 中学生でも理解できる平易な日本語。専門用語は避けるか、やさしく言い換える。
- 不安を和らげる、思いやりのある表現を使う。
- 印刷して患者さんに渡せる体裁（見出し・箇条書きで読みやすく）。

【出力フォーマット — Markdown】
# （テーマ）について

## これはどんなもの？
（やさしく2〜3文）

## 大切なポイント
- （ポイント1）
- （ポイント2）
- （ポイント3）

## ご家庭で気をつけること
- （注意1）
- （注意2）

## よくあるご質問
**Q. （質問）**
A. （回答）

## 気になることがあれば
（受診・相談を促す、安心できる一言）

【厳守】断定/最大級表現は禁止。「個人差があります」を要所で。出力はMarkdown本文のみ（前置き・コードフェンス禁止）。`;
}
